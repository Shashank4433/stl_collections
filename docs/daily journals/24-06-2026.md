# Daily Journal

**Date:** 24-06-2026

## Summary

Today's work focused on completing the implementation of the `LinkedList` template container and initiating development of the `HashMap` data structure. The LinkedList implementation was completed with support for insertion, deletion, searching, deep-copy operations, and proper memory management. Development of the HashMap structure began with bucket initialization, node design, and basic insertion mechanisms using separate chaining.

---

## LinkedList Development

### Core Structure

* Created the `LinkedList<T>` template class.
* Implemented the `Node<T>` structure.
* Added data members:

  * `head`
  * `tail`
  * `currentSize`
* Implemented the default constructor.
* Implemented:

  * `insertFront()`
  * `insertBack()`
  * `size()`
  * `isEmpty()`
  * `display()`
* Tested insertion operations using integer values.

### Advanced Operations

Implemented the remaining LinkedList functionality:

* `deleteFront()`
* `deleteBack()`
* `insert(index, value)`
* `remove(index)`
* `get()`
* `set()`
* `find()`
* `contains()`
* `search()`
* `clear()`

### Copy Semantics and Memory Management

Implemented:

* Destructor
* Copy Constructor
* Copy Assignment Operator

Verified:

* Proper memory cleanup
* Deep-copy behavior
* Independent object ownership
* Safe deletion of dynamically allocated nodes

### Testing

Performed testing using:

* `int`
* `std::string`

Verified:

* Front and back insertion
* Indexed insertion and removal
* Searching operations
* Deep-copy functionality
* Error handling for invalid indices
* Operations on empty lists

---

## Challenges Faced

### LinkedList

Special care was required for:

* Inserting the first node into an empty list.
* Updating both `head` and `tail` correctly.
* Deleting the final remaining node.
* Preventing memory leaks during deletion.
* Ensuring copied lists maintain separate memory allocations.

All edge cases were tested and resolved successfully.

---

## HashMap Development

### Initial Structure

Started implementation of the `HashMap<K,V>` template container.

Implemented:

* `HashMap<K,V>` class structure.
* `Node<K,V>` structure for storing key-value pairs.
* Dynamic bucket array:

  ```cpp
  Node<K,V>** buckets;
  ```
* Core variables:

  * `currentSize`
  * `currentCapacity`
* Default capacity initialization.
* Bucket initialization with `nullptr`.

### Hash Function Design

* Designed the hash function framework.
* Implemented bucket index calculation logic.
* Studied key-to-bucket mapping mechanisms.

### Collision Handling

* Selected Separate Chaining for collision resolution.
* Designed linked-list-based bucket storage.
* Implemented basic insertion into empty buckets.

### Testing

* Verified bucket initialization.
* Tested memory allocation for bucket arrays.
* Checked hash index calculations.
* Inserted sample key-value pairs successfully.

---

## Key Learnings

* Improved understanding of dynamic memory management in linked structures.
* Gained experience implementing deep-copy functionality for template containers.
* Learned how bucket-based storage works in hash tables.
* Explored collision handling using separate chaining.
* Studied hash function design and bucket distribution concepts.

---

## Results Achieved

### LinkedList

* Complete implementation finished.
* Deep-copy functionality verified.
* Memory management validated.
* Integer and string testing completed successfully.

### HashMap

* Basic structure completed.
* Node design finalized.
* Bucket array initialization completed.
* Constructor implemented.
* Hash function framework created.
* Basic insertion verified.

---

## Build Status

### LinkedList

* Compilation Successful
* All operations implemented
* Deep-copy functionality verified
* Memory cleanup validated
* Multi-type testing completed

### HashMap

* Compilation Successful
* Structure created
* Node implementation completed
* Bucket initialization verified
* Basic insertion functionality working

---

## Next Steps

### HashMap

Planned tasks:

* Complete collision handling implementation.
* Implement `get()` operation.
* Implement `remove()` operation.
* Implement `update()` functionality.
* Add load factor calculation.
* Implement rehashing support.
* Add copy constructor and assignment operator.
* Perform comprehensive testing using multiple key and value types.
